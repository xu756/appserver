syntax = "proto3";

package pb;
option go_package = "./pb";

message WsData{
  string  ImId = 1;
  bytes  ImData = 2;
}
message WsResp{
  bool Ok = 1;
}

service ImService {
  // 其他服务调用发送消息
  rpc Send(WsData) returns (WsResp) {}
  // 其他服务调用广播消息
  rpc Broadcast(WsData) returns (WsResp) {}
}
